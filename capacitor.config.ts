import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'ionicxperiments',
  webDir: 'www',
  server: {
    androidScheme: 'https'
  }
};

export default config;
